namespace Models.Enums;

/// <summary>
/// An enum containing device types.
/// </summary>
public enum DeviceType : byte
{
    Camera = 1,
    WindowShade = 2,
    Lightbulb = 3,
    Doorbell = 4,
    Other = 5,
}